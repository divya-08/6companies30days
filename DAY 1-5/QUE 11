// { Driver Code Starts
#include <bits/stdc++.h>

using namespace std;

 // } Driver Code Ends
class Solution{
public:
    int *findTwoElement(int *arr, int n) {
        int *ans=new int[2];
        sort(arr, arr+n);
        for(int i=0;i<n-1;i++) {
            if(arr[i] == arr[i+1]) {ans[0]=arr[i]; arr[i]=0;}
        }
        sort(arr, arr+n);
        int i;
        for(i=0;i<n;i++) {
            if(arr[i]!=i) {
                ans[1] = i;
                break;
            }
        }
        if(i==n) {ans[1]=n;}
        return ans;
    }
};

// { Driver Code Starts.

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n;
        cin >> n;
        int a[n];
        for (int i = 0; i < n; i++) {
            cin >> a[i];
        }
        Solution ob;
        auto ans = ob.findTwoElement(a, n);
        cout << ans[0] << " " << ans[1] << "\n";
    }
    return 0;
}  // } Driver Code Ends
